# Install with Kvantify's private index appended:
#   pip install -r requirements.txt --extra-index-url <KVANTIFY_INDEX_URL>
#
# The index URL is an entitlement credential. It is never committed here — workshop
# participants get it applied automatically during instance provisioning, and standalone
# users get it from Kvantify when they register.

# Proprietary, from Kvantify's private index.
# Pinned exactly, for two reasons:
#   1. Reproducibility — every participant gets the version that was tested.
#   2. The name `qrunch` is unregistered on PyPI, and provisioning installs with
#      --extra-index-url, which consults PyPI as well. An exact pin limits what a
#      squatter registering that name could substitute.
qrunch==1.3.0

# Public, from PyPI.
#
# amazon-braket-sdk is deliberately NOT pinned here. qrunch pins it transitively
# (1.3.0 requires >=1.119.1) and pinning it independently makes the install
# unresolvable — that mistake is what broke provisioning on 2026-09-07. If you need
# a specific SDK version, change the qrunch pin to match rather than adding one here.
matplotlib>=3.8
ipykernel>=6.29
ipywidgets>=8.1
